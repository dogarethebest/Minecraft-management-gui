# Minecraft-management-gui

it can be easily installed by running the installer script, the installer script has the flags 
--BYRR bypass ram requirements. This is a unsupported config and not recommended, but this will bypass RAM checks And unexpected behavior can happen
--BYRR this will bypass the root requirement This basically will break everything and will prevent anything from working this is a unsupported config and not recommended
--NOSERVICE this will prevent the installer from creating a service that will automatically start the server on boot 